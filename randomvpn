#!/bin/bash

PATH=$PATH:/etc/randomVPN

if [[ $# -eq 0 ]]
then
	if ifconfig | grep "tun" &> /dev/null
	then
		./errorlog "TunnelOpen"
	else
	        ./connectTunnel
	fi
elif [[ $1 = "-l" ]]
then
        sudo ./showlogs
fi
